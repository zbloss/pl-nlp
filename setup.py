from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='playing around with pytorch lightninig and nlp models, possibly integrating deepspeed',
    author='zach bloss',
    license='MIT',
)
